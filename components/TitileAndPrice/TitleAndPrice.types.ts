export interface ITitleProps {
  dataIndex: number;
  dataCurrent: number;
  dataTitle: string;
  dataColor: string;
  dataTextColor: string;
}
export interface IPriceProps {
  dataIndex: number;
  dataCurrent: number;
  dataPrice: number;
  dataColor: string;
}
